# Bandit Level 3 → Level 4

## Goal

Find the password for the next level.

The password is stored in a hidden file located inside the `inhere` directory.

## Commands Used

```bash
ls
cd inhere
ls
ls -la
cat "...Hiding-From-You"